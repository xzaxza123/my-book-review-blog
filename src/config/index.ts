/**
 * 项目统一配置文件
 * 
 * 本文件集中管理项目中所有需要配置的常量、键值、路径等配置项。
 * 所有配置项都包含详细的中文注释，方便理解和维护。
 */

// ============================================================================
// 虚拟内容 ID 配置
// ============================================================================

/**
 * 虚拟内容 ID：用于在起始页映射与目录中表示特殊内容
 */
export const VIRTUAL_IDS = {
  /** 前言页的虚拟 ID */
  PREFACE: "__preface__",
  /** 目录页的虚拟 ID */
  TOC: "__toc__",
} as const;

// ============================================================================
// 书本页面配置
// ============================================================================

/**
 * Turn.js 翻页库相关配置
 */
export const BOOK_PAGE_CONFIG = {
  /**
   * 内容页起始偏移量
   * turn.js 中 page 1/2 是封面相关页，实际内容从第 3 页开始
   * 因此需要设置偏移量为 2
   */
  TURN_CONTENT_OFFSET: 2,

  /**
   * 实际内容起始页码
   * 让首个内容页真正落在第 3 页（封面占用了前 2 页）
   */
  ACTUAL_CONTENT_START: 2,

  /**
   * 书本基准尺寸（像素）
   * 用于计算页面布局和分页
   */
  BASE_WIDTH: 500,
  BASE_HEIGHT: 622,
} as const;

/**
 * 前言页配置
 */
export const PREFACE_CONFIG = {
  /** 是否启用前言页 */
  enabled: true,
  /**
   * 前言页的逻辑页数（内容页数量，不含封面）
   * 必须为偶数；如为奇数会在运行时向上取偶
   */
  totalPages: 2,
} as const;

// ============================================================================
// 目录配置
// ============================================================================

/**
 * 目录相关配置
 */
export const TOC_CONFIG = {
  /**
   * 每页显示的目录项数量
   * 用于计算目录总页数和分页显示
   */
  ITEMS_PER_PAGE: 10,
} as const;

// ============================================================================
// 动画配置
// ============================================================================

/**
 * 页面过渡动画配置
 */
export const ANIMATION_CONFIG = {
  /**
   * 进入动画持续时间（毫秒）
   * 书本页面进入时的动画时长
   */
  ENTER_DURATION: 1200,

  /**
   * 退出动画持续时间（毫秒）
   * 书本页面退出时的动画时长
   */
  EXIT_DURATION: 800,
} as const;

// ============================================================================
// 分页配置
// ============================================================================

/**
 * 内容分页相关配置
 */
export const PAGINATION_CONFIG = {
  /**
   * 最小剩余空间阈值（像素）
   * 当前页剩余空间至少达到此高度才尝试对元素做"部分填充"拆分
   * 避免无意义断行，提升分页质量
   */
  MIN_REMAINING_TO_SPLIT: 10,
} as const;

// ============================================================================
// 存储键配置
// ============================================================================

/**
 * 本地存储键名配置
 * 统一管理所有 localStorage 和 sessionStorage 的键名
 */
export const STORAGE_KEYS = {
  /**
   * 文章起始页映射的存储键
   * 用于在 sessionStorage 中存储文章 ID 到起始逻辑页码的映射关系
   * 便于跨次打开时快速恢复目录信息
   */
  ARTICLE_START_PAGES: "book-article-start-pages",

  /**
   * 文章分页缓存的存储键前缀
   * 用于在 sessionStorage 中存储文章的分页结果
   * 通过版本号（v2）可以在分页算法调整后自动失效旧缓存
   * 格式：{PREFIX}{articleId}
   */
  PAGINATION_PREFIX: "book-pagination:v3:",

  /**
   * 首页交互提示的存储键
   * 用于在 localStorage 中记录用户是否已查看过首页的交互提示
   */
  HOME_INTERACTION_HINTS: "home-interaction-hints",

  /**
   * 主题设置的存储键
   * 用于在 localStorage 中存储用户选择的主题（light/dark）
   */
  THEME: "my-book-review-theme",
} as const;

// ============================================================================
// 路由配置
// ============================================================================

/**
 * 应用路由配置
 */
export const ROUTES = {
  /**
   * 书本阅读页面的路由路径
   * 用于导航到书本视图的入口路由
   */
  BOOK: "/MyBookReview",
} as const;

// ============================================================================
// Turn.js 翻页库配置
// ============================================================================

/**
 * Turn.js 翻页库的默认配置选项
 * 这些配置会传递给 turn.js 库进行初始化
 */
export const TURN_JS_CONFIG = {
  /**
   * 是否启用硬件加速
   * 启用后可以提升翻页动画的性能
   */
  acceleration: true,

  /**
   * 默认显示模式
   * - 'single': 单页模式
   * - 'double': 双页模式（默认）
   */
  display: "double" as "single" | "double",

  /**
   * 翻页动画持续时间（毫秒）
   * 控制翻页动画的播放时长
   */
  duration: 600,

  /**
   * 起始页码
   * 书本打开时默认显示的页码
   */
  page: 1,

  /**
   * 是否启用渐变效果
   * 翻页时的视觉渐变效果
   */
  gradients: true,

  /**
   * 用于翻页的角落
   * 'bl,br' 表示左下角和右下角可以触发翻页
   */
  turnCorners: "bl,br" as const,

  /**
   * 事件回调函数对象
   * 可以在这里配置翻页事件的处理函数
   */
  when: null as any,
} as const;

/**
 * Turn.js 翻页效果配置
 */
export const TURN_JS_FLIP_CONFIG = {
  /**
   * 每个角落的活动区域大小（像素）
   * 控制鼠标/触摸在角落区域多大范围内可以触发翻页
   */
  cornerSize: 100,
} as const;

/**
 * Turn.js DOM 优化配置
 */
export const TURN_JS_DOM_CONFIG = {
  /**
   * DOM 中保留的页面数量
   * 最小值为 6，用于性能优化
   * 只渲染当前可见页面及附近的页面，减少 DOM 节点数量
   */
  pagesInDOM: 6,
} as const;

// ============================================================================
// 导出便捷访问器（可选）
// ============================================================================

/**
 * 获取文章分页缓存的存储键
 * @param articleId 文章 ID
 * @returns 完整的存储键名
 */
export function getPaginationStorageKey(articleId: string): string {
  return `${STORAGE_KEYS.PAGINATION_PREFIX}${articleId}`;
}

